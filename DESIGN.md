# matakore（またこれ）— DESIGN.md

> スーパーの棚の前で「これ、前も買ったっけ？また買うほど美味しかったっけ？」に3秒で答えるための個人用アプリ。

---

## 1. コンセプト

### 1.1 解きたい課題

食品・日用品は「買った記憶」と「美味しかった記憶」が数週間で消える。結果として

- 微妙だった商品を再び買ってしまう
- 良かった商品の名前を思い出せず買い逃す
- 「このメーカーのカレーは大体好み」という傾向が言語化されない

### 1.2 プロダクトの一行定義

**店頭でバーコードを読むと「買う／やめる」の判断材料が即座に出る、個人の食品評価データベース。**

### 1.3 設計上の最重要トレードオフ

このアプリの成否は **データが貯まるかどうか** に完全に依存する。したがって全設計判断は次の優先順位に従う。

1. **入力の摩擦を減らす** — 1商品あたり5秒を超える入力フローは設計として失敗
2. **参照が速い** — 店頭・オフライン・片手で完結する
3. 分析・推薦機能 — 上2つが成立して初めて意味を持つ

「情報をいっぱい集めたい」と「間口を簡単にしたい」は本質的に対立する。本設計ではこれを **入力を時間軸で2分割する** ことで解決する（§3.1）。

---

## 2. 名前

**matakore / またこれ**

- 「また、これ買う？」という、このアプリが答える唯一の問いをそのまま名前にした
- 肯定文脈（「またこれでいいや」＝定番化）と疑問文脈の両方を含む
- パッケージ名・リポジトリ名として短くASCIIで済む

代替案: `Repiy`（リピ買い）／ `Nidome`（二度目）

---

## 3. コアUX

### 3.1 入力を2フェーズに分割する

一度の操作で「商品情報＋評価」を取ろうとすると必ず破綻する。買う瞬間には評価がまだ存在しないため。

| フェーズ | 場所 | 操作 | 所要 | 得られるデータ |
|---|---|---|---|---|
| **Phase A: 捕獲** | 店頭・帰宅直後 | バーコードをスキャンするだけ | 2秒 | JAN、購入日、（任意で価格・店舗） |
| **Phase B: 評価** | 食べた後・いつでも | 未評価キューから1タップ | 3秒 | リピ意思、メモ、タグ |

Phase A は思考ゼロで完了する。Phase B は溜まっても構わない設計にし、「未評価が3件」のような軽い催促だけを出す。

### 3.2 判定モード（メインの利用シーン）

スキャン後、1画面で完結させる。スクロールなし。

```
┌────────────────────────────┐
│  ハウス こくまろカレー 中辛      │
│  ハウス食品 / ルウ・カレー       │
├────────────────────────────┤
│  ◎ また買う                     │  ← 最大の要素。色とアイコンで即判別
│                                │
│  3回購入  最終 2026-06-14       │
│  「甘めだけど子供っぽくない。      │
│    常備でいい」                 │
├────────────────────────────┤
│  同カテゴリの記録                │
│  ・S&B ゴールデン  △ 微妙        │
│  ・こくまろ 辛口   ◎ また買う     │
└────────────────────────────┘
```

未登録商品だった場合は、代わりに **同カテゴリ／同メーカーの過去評価** を出す。これにより「この商品は初めてだが、このメーカーのパスタソースは2回とも微妙だった」という判断ができる。単純な有無判定より価値が高い。

### 3.3 評価スケールの設計

星5段階は採用しない。店頭で必要な情報は「また買うか」の一点であり、星3.5と星4の区別は判断に寄与しない。

**主軸: repeat intent（4値）**

| 値 | 表示 | 意味 |
|---|---|---|
| `staple` | ◎ 定番 | 切らしたら買う |
| `yes` | ○ また買う | 見かけたら買う |
| `meh` | △ 微妙 | あえて買わない |
| `no` | ✕ もういい | 二度と買わない |

**副次情報（すべて任意）**

- 一行メモ（最も価値が高い。「甘い」「量が少ない」など未来の自分への申し送り）
- 自由タグ（`辛い` `常備` `子供受け` `高い`）
- 星（他人に薦める用途が生まれたときのため。当面は非表示でも良い）

---

## 4. バーコード読み取り

### 4.1 スキャン実装

対象は JAN（EAN-13 / EAN-8）。国内食品はほぼこれで足りる。

- 第一選択: **Barcode Detection API**（`BarcodeDetector`）— ネイティブ実装で高速・省電力
- フォールバック: **zxing-wasm**（`@sec-ant/zxing-wasm` 等）を動的 import

iOS Safari は `BarcodeDetector` を実装していない期間が長く、現時点でも前提にできない。**wasm フォールバックは必須実装であり、後回しにするとiPhone実機で詰む。** 検出後は Vibration API か短い効果音で成功を返す（画面を見なくてもスキャンできる）。

### 4.2 商品マスタの解決（カスケード）

JAN から商品名・メーカー・画像を引く。単一の万能ソースは存在しないため多段フォールバックにする。

| 段 | ソース | 特性 |
|---|---|---|
| 1 | **ローカル IndexedDB** | 既知商品。即応・オフライン可 |
| 2 | **自前共有マスタ**（Cloudflare D1） | 過去に解決したJANのキャッシュ。API呼び出しを削減 |
| 3 | **Yahoo!ショッピング 商品検索v3** | JANコード指定検索に対応。国内食品のカバレッジが最も広い |
| 4 | **楽天市場 商品検索API** | Yahooで取れない商品の補完 |
| 5 | **Open Food Facts** | オープンデータで栄養情報が取れるが、**日本の食品カバレッジは薄い**。補助扱い |
| 6 | **手入力** | 商品名だけ打てば登録完了。ここで詰まらせない |

**設計上の注意**

- ECのAPIは「商品」ではなく「出品」を返す。同一JANに対しノイズの多い店舗名入りタイトル（`【送料無料】ハウス こくまろカレー 中辛 140g×5個 まとめ買い`）が返るため、**正規化処理（記号・煽り文句・入数の除去）を前提に組む**
- APIキーはクライアントに置けない。解決処理は必ず Workers 側のプロキシ経由にする（§6）
- 取得した商品情報は **スナップショットとしてローカルに保存** する。外部APIが将来死んでも自分の記録は失われない

---

## 5. データモデル

**個人の評価（自分の資産）と商品マスタ（外部由来）を厳密に分離する。** マスタは再取得可能だが評価は代替不能であり、バックアップとエクスポートの単位も異なる。

```ts
// 商品マスタ（外部APIまたは手入力由来。再取得可能）
type Product = {
  jan: string;              // 主キー。EAN-13
  name: string;             // 正規化後の商品名
  rawName?: string;         // API生データ（正規化ミスの復元用）
  brand?: string;           // ハウス食品
  imageUrl?: string;
  categoryId: string;       // → Category
  source: 'yahoo' | 'rakuten' | 'off' | 'manual';
  fetchedAt: number;
};

// 購入イベント（何回買ったかを追う）
type Purchase = {
  id: string;
  jan: string;
  purchasedAt: number;
  price?: number;
  store?: string;
};

// 評価（アプリの中核資産。商品に対して1件、更新可）
type Review = {
  jan: string;              // 主キー
  intent: 'staple' | 'yes' | 'meh' | 'no';
  memo?: string;
  tags: string[];
  stars?: number;
  updatedAt: number;
  history: { intent: Review['intent']; at: number }[];  // 心変わりも情報
};

// カテゴリ（2階層固定。深くしない）
type Category = {
  id: string;
  major: '食品' | '飲料' | '日用品' | '菓子';
  minor: string;            // カレー / パスタソース / カップ麺 / 冷凍餃子
};
```

### 5.1 カテゴリ設計

**外部APIのジャンル情報は使わない。** ECのカテゴリツリーは販売都合で切られており、「カレー」がレトルトとルウで別枝になるなど、味の傾向を比較したい粒度と一致しない。

自前で以下を持つ。

- **2階層固定**（大分類・中分類）。3階層以上にすると入力時に迷って摩擦になる
- 中分類は初期50件程度をシードし、必要に応じて追加
- 自動分類は「商品名キーワードルール → 一致しなければLLM補完 → 最後に手動確認」の順。**確定前に必ずユーザーが1タップで承認できる導線を置く**（誤分類は推薦機能の品質を直接壊すため）
- 横断的な性質はカテゴリではなく**タグ**で表現する（`辛い` `常備` `お土産`）

---

## 6. アーキテクチャ

### 6.1 方針

- **PWA**。ネイティブアプリはストア審査と証明書更新のコストが個人用途に見合わない
- **オフラインファースト**。スーパーの店内、特に地下や大型店の奥は電波が期待できない。**判定は完全にローカルデータだけで成立させる**
- **ランニングコストゼロ**。無料枠内に収める

### 6.2 構成

```
┌──────────────────────────────┐
│  PWA (React + TS + Vite)         │
│  ├ zxing-wasm / BarcodeDetector  │
│  ├ Dexie.js (IndexedDB)  ← 全件  │  ← ここだけで判定が完結する
│  └ Service Worker (オフライン)    │
└───────────┬──────────────────┘
            │ 差分同期 / マスタ解決のみ
┌───────────▼──────────────────┐
│  Cloudflare Workers              │
│  ├ /resolve/:jan  外部APIプロキシ  │  ← APIキーはここに隔離
│  ├ /sync          差分同期        │
│  └ Cron Triggers  夜間バッチ      │  ← カテゴリ自動分類（LLM）、
│                                 │     傾向分析の事前計算、定期エクスポート
│  D1: 商品マスタキャッシュ + バックアップ│
└──────────────────────────────┘
```

Workers は薄いプロキシと同期に徹し、バッチ処理は Cron Triggers で同じ Worker 内に置く。無料枠の CPU 時間制限（1リクエストあたり数十ms）に収まらない処理が出てきた時点で、初めて別の実行環境を検討する。

### 6.3 認証

単一ユーザー。Cloudflare Access もしくは固定トークン + Workers 側検証で十分。ログイン画面をアプリ内に作らない（起動から判定までの秒数を1秒でも削る）。

### 6.4 データポータビリティ

数年かけて貯める前提のデータなので、**JSON / CSV への完全エクスポートを最初のリリースに含める**。後付けにすると構造が複雑化して実装されないまま塩漬けになる。

---

## 7. 推薦機能（将来）

データが貯まる前に手を出さない。50件では何も出ないし、出たとしても検証できない。段階を踏む。

### Phase 1: 集計（記録100件〜）

MLではなく単純な集計。これだけで実用価値がある。

- カテゴリ × メーカーの「また買う率」
- タグ別の傾向（`辛い` タグの商品は78%が `yes` 以上 → 辛いものが好き）
- 「まだ評価していないカテゴリ」の提示

### Phase 2: 傾向の言語化（記録300件〜）

タグ・メモ・カテゴリを LLM に渡し、嗜好プロファイルを自然文で生成する。「甘口より中辛、価格より内容量を重視、冷凍食品は総じて評価が低い」といった出力。Workers の Cron Triggers から週次で実行。

### Phase 3: 類似商品推薦（記録500件〜）

商品名・タグ・カテゴリから埋め込みベクトルを作り、`staple` / `yes` 群との近傍で未評価商品をスコアリング。店頭スキャン時に「未購入だが好みに近い」を出す。

**制約の明示**: 自分の評価データだけでは「まだ知らない良い商品」は原理的に推薦できない。カバレッジを広げるには外部の商品カタログ（EC APIの新商品一覧など）を候補プールとして取り込む必要がある。Phase 3 の設計時に改めて判断する。

---

## 8. ロードマップ

| Phase | 内容 | 完了条件 |
|---|---|---|
| **0** | スキャン → 手入力 → ローカル保存 → 判定表示 | 外部API抜きで実店舗で1回使い切れる |
| **1** | JANマスタ解決（Workers プロキシ）、カテゴリ、未評価キュー | スキャンだけで商品名が出る |
| **2** | PWA化・オフライン対応・D1同期・エクスポート | 電波なしで判定できる／機種変更で消えない |
| **3** | 集計ダッシュボード、タグ分析 | 傾向が可視化される |
| **4** | LLM嗜好プロファイル、類似商品推薦 | — |

Phase 0 を外部API抜きで切っているのは意図的。**APIカバレッジという最も不確実な要素を、コア体験の検証から切り離すため。** ここで「そもそも自分がスキャンする習慣を作れるか」を先に検証する。

---

## 9. 未決事項

- **レシート取り込み**: Phase A（捕獲）をさらに省力化できる。レシートをOCRして購入イベントを一括登録し、JAN照合できたものだけ商品に紐付ける。ただしレシートの商品名は極端に省略されている（`ハウスコクマロカラーチュ`）ため照合精度が読めない。Phase 3以降に実験
- **同一商品の別JAN**: リニューアルや内容量違いでJANが変わる。`ハウス こくまろカレー 中辛` として名寄せするか、別商品として扱うか。前者は「シリーズとしての評価」を可能にするが正規化が難しい。当面は別商品として持ち、名寄せは後段のビュー層で行う
- **評価の鮮度**: 3年前の `yes` は今も有効か。`history` は持つが、UIでどう減衰させるかは未定
- **他人の記録との共有**: 現時点では単一ユーザー前提。共有はスコープ外とするが、データモデル上 `Review` にユーザーIDを持たせる余地は残しておく
